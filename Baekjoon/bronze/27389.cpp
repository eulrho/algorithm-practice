// Metronome
#include <iostream>
#define fast ios::sync_with_stdio(false), cin.tie(NULL), cout.tie(NULL);
using namespace std;

int main()
{
	fast;
	double n;

	cin >> n;
	cout << fixed;
	cout.precision(2);
	cout << n / 4;
	return (0);
}